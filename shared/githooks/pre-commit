#!/bin/sh
# 分支守卫的 git 层入口。Claude Code 的 PreToolUse hook 拦 agent 经 Bash 发的命令，
# 这个拦本机的 git commit —— **只有 commit**：merge 走 pre-merge-commit、
# rebase/cherry-pick/revert/am 走分离头指针、push 完全不经过这里，都管不到。
# 逻辑在隔壁的 guard-branch.js 里，两边共用一份。
#
# **这份文件跟着 dev-infra 走**：各消费仓把这个仓库当 git submodule 挂在
# vendor/dev-infra 下，所以这一份就是各仓实际执行的那一份，不再有副本。
#
# **下面那行是相对这个文件自己算的**，所以挂在哪个路径下都成立——
# 这正是它上一版栽过的地方：那时写死的是 ../scripts/guard-branch.js，
# 而钩子一换目录（shared/ → shared/githooks/）那条路径就指向了不存在的文件。
# 要改这一行之前先想清楚：guard-branch.js 和这个文件的相对位置变了没有。
#
# 它还必须有可执行位，否则 git 直接跳过、**不报任何错**。
# 那一位由这个仓库的对象库直接载着（100755），submodule-shape.test.js 钉着它。
exec node "$(dirname "$0")/../guard-branch.js" --pre-commit
